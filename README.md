# P ACT-R Interactive Graph

Interactive graph for the formula:

```text
P = 1 / (1 + ((Time_i * p * e^(f(t + S_p))) / F)^(1 / f_s))
```

Open `index.html` in a browser to use the sliders and inspect how each parameter changes `P`.
